#include <stdio.h>
int main(){
	int array[100];
	int a, count = 0; 
	while(1){
		scanf("%d", &a);
		if(a>100){
			break;
		} else {
			array[count]=a;
			count++;
		}
	} 
		for(int i=a; i<=100; i++){ 
		int a = count;
			printf("sum: %d", i);
		 
		 
		}
		
		
		
		
}

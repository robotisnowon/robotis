#include <stdio.h>
int main (){	
	int array[100];
	int a, count=0;
	while(1){
		scanf("%d", &a);
		if(a==0) break;
		array[count]=a;
		count++;
	
	}
	
			if(count/2){
		printf("even :");
	} else {
		printf("odd :");
	
}	
	}
